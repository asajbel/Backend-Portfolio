<header class="bg-light">
  <nav class="navbar container navbar-expand-lg navbar-light p-0">
    <h1 class="navbar-brand text-light bg-teal">Anders Sajbel</h1>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item ml-auto {{#if home}}active{{/if}}">
          <a class="nav-link ml-auto" href="/">About Me
            {{#if home}}<span class="sr-only">(current)</span>{{/if}}</a>
        </li>
        <li class="nav-item ml-auto {{#if portfolio}}active{{/if}}">
          <a class="nav-link" href="/portfolio">Portfolio
            {{#if portfolio}}<span class="sr-only">(current)</span>{{/if}}</a>
        </li>
        <li class="nav-item ml-auto {{#if contact}}active{{/if}}">
          <a class="nav-link" href="/contact">Contact
            {{#if contact}}<span class="sr-only">(current)</span>{{/if}}</a>
        </li>
      </ul>
    </div>
  </nav>
</header>